name: "Auto commmit and push"
description: "Add files to a new commit and pushes it. Current implementation is dummy, only list files to commit."
author: "Yeray David Rodriguez"
inputs:
  job_state:
    description: "A comma separated list of files, enclosed in brackets"
runs:
  using: "docker"
  image: "Dockerfile"

