name: "Copy to Base Images Folder"
description: "Copy base files to its destination folder"
author: "Yeray David Rodriguez"
inputs:
  job_state:
    description: "JSON structure containing 'icc profile modify output' attribute with files to copy"
    required: true
outputs:
  result:
    description: "The modified input structure"
runs:
  using: "docker"
  image: "Dockerfile"

